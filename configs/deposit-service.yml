spring:
  application:
    name: deposit-service
  data:
    mongodb:
      database: DepositService
      port: 27017
      host: localhost
server:
  port: 8004
eureka:
  client: 
    serviceUrl:
      defaultZone: https://localhost:8090/eureka/
