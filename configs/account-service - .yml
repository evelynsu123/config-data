spring:
  application:
    name: deposit-service
  data:
    mongodb:
      database: DepositService
      port: 27017
      host: localhost
server:
  port: 8002
eureka:
  client: 
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
